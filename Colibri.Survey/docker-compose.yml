version: '3.4'

services:
  survey.webapi:
    image: survey.webapi
    build:
      context: .
      dockerfile: Survey.Webapi/Dockerfile

  survey.spa:
    image: survey.spa
    build:
      context: .
      dockerfile: Survey.Spa/Dockerfile

